<!-- README First protocol v2.2.0 -->

# AGENTS.md

## 总纲

本项目的 AI 协作原则是：**先理解，再行动；先收敛不确定性，再修改文件；协议重量与任务影响成正比，专业门禁与风险领域相匹配。**

Agent 的任务不是最快地产生代码，而是在**最小误解、最小破坏、最小副作用**的前提下完成用户目标，同时不让低风险任务支付高风险任务的流程成本。

这一原则由三条互补协议支撑：

- **README First 原则**：动手前按需读取项目上下文，读到“够用”为止，而不是“读完”为止。
- **本质不确定性最小化协议**：读完上下文后，真正理解目标、边界、风险和验收标准。
- **风险领域路由协议**：任务级别决定流程重量；风险领域决定需要加载的 Profile、Skill、检查清单和验证证据。

---

## 0. 最小闭环（每次任务必读，其余章节按需查阅）

每次任务只做四件事：

```text
定级（L0 / L1 / L2）
  → 识别风险领域并加载已安装的 Profile / Skill
  → 按级执行
  → 按级记录与报告
```

| | L0 轻量 | L1 标准 | L2 高影响 |
|---|---|---|---|
| 典型任务 | 查询问答、错别字、注释、文案、局部样式、显而易见的单文件小修 | 单模块内的常规功能与修复，不触及公共接口 | 公共 API、共享类型、数据结构、配置、权限、安全、迁移、跨模块或架构调整 |
| 必读 | 最近一级目录 README（如有） | 目标路径各级 README + 相关代码、测试 | L1 全部 + `.ai/architecture/` + 必要时 `.ai/changes/`、`.ai/decisions/` |
| 风险路由 | 通常不需要；若命中已安装 Profile 的明确触发条件则仍需加载 | 识别风险领域；命中本地 Profile 时加载 | 必须显式记录风险领域、Profile / Skill 和证据矩阵 |
| 任务契约 | 不需要 | 简版三项：目标 / 范围 / 验收 | 完整八项（见 3.2） |
| `.ai/changes/` | 不记录，commit message 足够 | 短模板（见 5.1） | 完整模板（见 5.1） |
| README 更新 | 否 | 命中触发条件时更新（见 5.2） | 必须逐条核对触发条件 |
| 最终输出 | 一两句话 | 四项简报（见 7） | 完整报告（见 7） |

**三条元规则：**

1. 定级拿不准 → 按更高级别执行。
2. 执行中命中 L2 特征、出现中高风险不确定性，或发现新的高风险领域 → 立即升级并补齐要求。
3. Profile、Skill 和扫描脚本只能增强项目本地规则，不能覆盖更严格的 `AGENTS.md`、目录 README、代码或测试事实。

---

## 1. 任务分级

任务级别回答：**这次修改需要多重的上下文、记录、验证和报告？** 它不替代风险领域识别。

**L0（轻量）**须同时满足：

- 只读操作，或改动局限于 1–2 个文件内的显而易见修改；
- 不触及公共接口、导出、数据结构、配置、权限、安全、迁移或持久化行为；
- 不改变任何目录职责或长期约定；
- 出错后可通过一次 revert 完全恢复。

**L1（标准）**：单个模块或目录内的常规开发任务；对外行为变化在用户明确要求的范围内；不修改被其他模块依赖的接口。

**L2（高影响）**，任一命中即是：

- 修改公共 API、共享类型、数据库 schema、状态流、路由、配置、构建或部署；
- 涉及权限、安全、支付、认证、隐私、数据迁移或生产发布；
- 修改事务边界、锁策略、唯一性约束、幂等、重试、消息发布或外部副作用；
- 修改多租户、对象级、动作级、字段级授权或缓存隔离；
- 引入或改变无界查询、批量、并发、上传、扇出、连接池或资源预算；
- 删除文件、废弃接口、跨模块修改、结构性重构；
- README、代码、测试之间存在影响本次实现的冲突；
- 存在高风险或不可逆的剩余不确定性（见 3.4）。

---

## 2. 读取规则：按需读取，够用即停

| 时机 | 读什么 |
|---|---|
| 会话内首次接触本项目 | 根 `README.md`（项目地图） |
| 每次任务 | 目标目录最近一级 README |
| L1 及以上 | 目标路径上各级 README（自上而下）+ 目标文件、直接依赖、调用方、相关测试 |
| 跨目录 / 架构 / 长期约定类任务 | `.ai/architecture/` |
| 需要理解历史原因时 | `.ai/changes/` 近期记录、`.ai/decisions/` 相关决策 |
| 会话开始时有未完成交接 | `.ai/handoff.md`（必读，仅次于第 0 节） |
| 项目存在术语表且任务涉及相关术语 | `.ai/glossary.md` |
| 命中一个或多个风险领域 | `.ai/profiles/` 中匹配的本地 Profile；Profile 指向的 Skill 主文件与必要 references |

补充规则：

1. **停止条件**：影响本次任务的偶然不确定性已经消除，并且已获得风险领域要求的最低证据后，停止扩大阅读。
2. **会话缓存**：会话内已读且未被修改的文档不必重读。
3. **缺失处理**：某级目录没有 README 时，向上用上级 README 谨慎推断，不得自行假设；关键目录且任务 ≥ L1 时，可用附录 A 的最小模板补建。
4. **Profile 缺失**：命中风险领域但项目未安装对应 Profile 时，不得编造规则；按项目事实完成通用风险分析，并在结果中说明能力包缺口。
5. **信任与漂移**：默认信任 README 和本地 Profile，只核对与本次修改直接相关的声明；发现文档与代码冲突时必须显式指出，不得静默选边。

### 优先级

```text
用户明确指令
> 项目 AGENTS.md
> 最近目录 README
> 上级目录 README
> 项目本地 .ai/profiles/
> 项目已安装的通用 Skill
> Skill references / 示例
> 根 README 与通用建议
```

代码、测试、schema、配置和真实运行结果是当前行为的事实来源。Profile 或 Skill 与项目事实冲突时，保留冲突并报告，不得为了“符合模板”而改写事实。

---

## 2.1 风险领域与能力路由

风险领域回答：**这次任务可能通过什么机制失败，需要哪类专业能力和证据？** 风险领域与 L0/L1/L2 相互独立，一项任务可以命中多个领域。

标准风险领域：

| 风险领域 | 典型触发 |
|---|---|
| `database` | SQL、ORM、索引、约束、数据模型、查询计划 |
| `transaction` | 事务边界、隔离级别、锁、死锁、原子性 |
| `concurrency` | 并发写、重复请求、竞态、幂等、配额与状态竞争 |
| `http-network` | HTTP 语义、CORS、Cookie、缓存头、超时、取消、重试 |
| `cache-overload` | 缓存分区与失效、连接池、限流、背压、过载保护 |
| `runtime-resources` | 事件循环、CPU、同步 I/O、内存、无界并发和批量 |
| `authentication-authorization` | 身份、租户、对象、动作、字段权限 |
| `security-privacy` | 敏感数据、日志、注入、SSRF、浏览器存储、数据保留 |
| `migration-release` | schema 迁移、兼容窗口、回填、停止、回滚、发布 |
| `external-side-effects` | 支付、消息、邮件、Webhook、第三方 API 与超时未知结果 |

路由步骤：

1. 从任务、目标文件、调用链、数据流和配置中识别风险领域。
2. 检查 `.ai/profiles/` 是否有匹配 Profile。
3. 读取 Profile 的触发条件、业务不变量、阻断条件和最低证据。
4. Profile 指向已安装 Skill 时，按 Skill 的相关模式执行；只加载本次需要的 references。
5. L2 任务把风险领域、使用的 Profile / Skill、业务不变量和对应证据写入 `.ai/changes/`。

约束：

- Scanner 命中只是候选线索，不是已确认 finding；无命中也不代表安全。
- Profile 是项目级路由契约，Skill 是可复用专业工作流；二者不能替代项目真实上下文。
- 不因“安装了 Skill”就对所有任务默认加载全部培训材料或检查清单。

---

## 3. 本质不确定性最小化协议

Agent 不得把用户 prompt 直接当作完整需求执行。本协议的目标不是消除所有不确定性，而是压缩**会导致错误修改**的那部分。

### 3.1 两类不确定性

- **偶然不确定性**：来自“还没读文件、没搜代码、没看测试”。必须优先用工程动作消除，不得转嫁给用户反复确认。
- **本质不确定性**：来自问题本身——用户真实目标、需求边界、业务规则、方案取舍、验收标准。靠收敛、假设和记录压缩。

### 3.2 任务契约

L1 在内部确认三项：**目标、范围、验收**。

L2 使用完整契约：

```text
1. 用户目标   2. 直接交付物   3. 影响范围   4. 非目标
5. 约束条件   6. 验收标准     7. 不确定性清单   8. 假设清单
```

若命中风险领域，再补充：

```text
9. 风险领域   10. 业务不变量与信任边界
11. 使用的 Profile / Skill   12. 风险对应证据
```

契约无需向用户展示全文，但最终回复必须包含与结果相关的关键假设、风险和验证证据。

### 3.3 压缩顺序

先消除偶然不确定性（读取）→ 识别本质不确定性 → 识别风险领域 → 用 README、代码、测试、类型、schema、调用方、Profile、changes 和 decisions 压缩 → 按 3.4 处理剩余不确定性。优先选择**最小、保守、可回滚**的方案。

### 3.4 剩余不确定性的风险分级

| 风险 | 判断标准 | 处理方式 |
|---|---|---|
| 低 | 不影响公共接口、核心逻辑、数据结构 | 基于明确假设继续，记录说明 |
| 中 | 可能影响局部行为，范围可控 | 最小变更 + 补充验证 + 记录假设 |
| 高 | 可能影响公共 API、数据、权限、安全、部署、核心流程 | 不得静默修改，明确指出风险与待确认问题 |
| 不可逆 | 删除核心文件、破坏兼容、迁移数据、大规模重构 | 未获明确确认不得执行 |

注意：本表描述**剩余不确定性的危险程度**，不是 2.1 的专业风险领域。

### 3.5 提问与假设

提问仅限阻塞性问题：读文档、代码、测试和已安装 Profile 仍无法解决，且显著影响实现方案或涉及中高风险。提问时给出默认方案；可安全推进的部分先完成。

**访谈模式**：当 L2 任务的本质不确定性主要来自用户意图、业务规则或取舍标准时，一次只问一个问题，说明影响哪个实现分支，沿决策树逐层收敛。结论写入任务契约，长期结论按 §5 沉淀。

必须基于假设继续时：假设要明确、保守、可验证，不扩大需求，不覆盖项目已有约定，不用于高风险或不可逆操作。

### 3.6 实现策略

最小必要变更；沿用现有架构与命名；复用而非新建抽象；保持兼容；局部修改优先于大范围重构。不得为让实现“看起来合理”而篡改 README；不得把未确认判断写成确定事实；不得添加用户未要求、README 未支持、测试未覆盖的额外功能。

若 `.ai/glossary.md` 存在，新增命名应使用其中术语。若命中 Profile，必须满足其阻断条件和最低证据，但不得机械应用与当前任务无关的全部检查项。

---

## 4. 增删改查检查清单

- **查**：基于 README、Profile、代码与运行事实回答，不凭记忆；发现冲突要指出。
- **增**：确认放置目录正确、无现成同类实现、有统一导出入口则走入口、命名和依赖符合约定；引入新依赖说明原因与影响。
- **改**：确认公共入口、调用方、类型、测试、配置和风险领域；高影响文件按 L2 处理。
- **删**：确认无引用残留、非受保护文件；删除一律按 L2 处理，除非用户明确要求，不得删业务代码来“简化结构”。

---

## 5. 记录与文档更新

### 5.1 分级记录 `.ai/changes/YYYY-MM-DD.md`

文件名精确到天，条目标题精确到时分，并标注级别。

L1 短模板：

```md
## HH:MM - <标题> [L1]
- 目标与修改：
- 原因：
- 风险领域：（无则写“无”）
- 假设 / 剩余不确定性：（无则写“无”）
- 验证证据：（命令 → 关键输出摘录 → 结论；无法运行写明原因与替代核查）
- 文档更新：（无则写“无”）
```

L2 完整模板：

```md
## HH:MM - <标题> [L2]
- 用户目标：
- 涉及目录：
- 风险领域：
- 使用的 Profile / Skill：（无则说明原因）
- 业务不变量与信任边界：
- 修改内容与原因：
- 已消除的不确定性：
- 关键假设：
- 剩余不确定性：
- 影响范围 / 风险控制：
- 风险对应证据：
  - 正确性：
  - 性能 / 资源：（不适用则写“不适用”）
  - 并发 / 重复：（不适用则写“不适用”）
  - 安全 / 隐私：（不适用则写“不适用”）
  - 迁移 / 恢复：（不适用则写“不适用”）
- README / Profile 更新：
- 是否新增/更新 `.ai/decisions/`：
```

**没有本次会话内新鲜验证证据，不得声称完成。**

产生长期架构决策 → 写入 `.ai/decisions/`；改变当前稳定架构、Profile 或能力包结构 → 同步检查 `.ai/architecture/`。

### 5.2 README 与 Profile 更新触发条件

命中任一，必须更新对应长期文档：新增或删除核心文件；目录职责变化；公共接口、数据结构、模块边界变化；依赖、构建、测试、运行方式变化；风险路由、Profile、Skill 或证据门禁变化；已修正文档与实现的不一致。

判断标准：

> 六个月后的 Agent 或新维护者是否需要知道这次修改背后的规则、边界或原因？需要 → 更新 README、Profile、architecture 或 decisions；不需要 → 不污染长期文档。

补充规则：

1. **时效戳**：更新目录 README 或 `.ai/architecture/` 时使用 `> 更新于:YYYY-MM-DD`。可选基线必须明确写成“核验基线”，不得把修改前 commit 冒充本文件所属提交。
2. **术语表触发**：L1 及以上任务中，反复出现且需要解释的领域概念写入 `.ai/glossary.md`。
3. **标准扩展**：`.ai/plans/`、`.ai/glossary.md`、`.ai/handoff.md`、`.ai/profiles/` 按需启用，不命中条件时无需创建。
4. **能力包**：通用能力包放在 canonical `extensions/`；安装到目标项目后，Profile 放 `.ai/profiles/`，Skill 放项目的 `skills/` 或宿主规定目录。

### 5.3 文档分工

| 机制 | 用途 |
|---|---|
| git diff / commit | 改了什么 |
| 目录 `README.md` | 当前稳定的目录契约 |
| `.ai/architecture/` | 当前稳定架构状态与跨目录边界 |
| `.ai/profiles/` | 项目本地风险路由、触发条件、最低证据和 Skill 绑定 |
| 项目 `skills/` | 可复用专业执行工作流、references 和 scripts |
| `.ai/changes/` | 为什么改、风险是什么、基于什么假设、如何验证 |
| `.ai/decisions/` | 为什么长期这样选 |

长期文档不记录流水账、临时调试、完整 diff 或未经确认的猜测。

### 5.4 计划协议（如触发）

**触发**：L2 且预计涉及 3 个以上文件或多阶段执行，或用户明确要求先出计划。

**标准**：计划写到“零上下文执行者也能照做”的粒度；每项标明文件、步骤、风险领域、验证命令和完成条件，用 checkbox 追踪。

**执行**：先批判性审查计划，再逐项勾选；完成后移入 `.ai/plans/done/`，changes 引用计划路径。

### 5.5 会话交接（如触发）

会话结束但任务未完成时，写 `.ai/handoff.md`：目标、已完成、当前状态、下一步、关键假设、风险领域、使用的 Profile / Skill、建议先读的文件。内容必须脱敏。

接续会话完成任务后清空；maintainer 发现超过 14 天的交接，列入待确认。

---

## 6. 禁止行为

1. 未读目标目录 README 就修改该目录文件。
2. 把模糊描述直接解释成确定需求。
3. 在高风险不确定性下静默修改核心文件。
4. 用大范围重构掩盖对需求理解不清。
5. 编造业务规则，或把猜测写入 README / Profile 伪装成事实。
6. 为减少工作量而忽略调用方、测试、README 或已安装 Profile。
7. 在没有本次会话内新鲜验证证据时声称问题已解决。
8. 删除或覆盖与任务无关的现有文档和代码。
9. 发现文档与代码冲突时静默选边。
10. 修改 README 或 Profile 以掩盖错误实现。
11. 低报任务级别或遗漏风险领域以规避流程。
12. 把扫描器命中直接包装成已确认缺陷，或把无命中包装成安全证明。
13. 把整个培训手册、所有 references 或所有 Skill 默认塞入每次任务上下文。

---

## 7. 最终输出

- **L0**：一两句话说明做了什么，或直接给出答案。
- **L1**：完成了什么 / 改了哪些文件 / 如何验证 / 关键假设与风险领域（如有）。
- **L2**：完成内容；修改文件；使用的上下文、Profile / Skill；风险领域与业务不变量；关键假设；已消除与剩余不确定性；风险对应证据；更新的长期文档。

如果没有修改任何文件，也必须说明原因和建议的下一步。

---

## 附录 A：目录 README 模板

补建时最少三节即可（职责、核心文件、测试与验证），后续由维护流程渐进补全：

```md
# 目录说明：<目录名>

> 更新于:YYYY-MM-DD

## 职责
本目录负责：
本目录不负责：

## 核心文件
| 文件/子目录 | 作用 |
|---|---|

## 约定与依赖边界
（命名、导出方式；可以依赖什么、不应依赖什么）

## 测试与验证
修改本目录后运行：

## 维护提示
（未来维护者需要知道的长期约定；没有就删除本节）
```

目录 README 应“短而准”，通常 50–150 行；复杂目录应拆分子目录或专题文档，而不是写成百科全书。
