## 09:41 - 升级 README First 至 v2 分级执行版 [L2]
- 用户目标:在保持最小不确定性原则下,大幅降低每次 Agent 任务的协议负担,并建立文档系统的定期维护机制。
- 涉及目录:根 `AGENTS.md`、根 `README.md`、`skills/readme-first-builder/`、`skills/readme-first-maintainer/`(新增)、`.ai/architecture/`。
- 修改内容与原因:引入 L0/L1/L2 任务分级,读取、契约、记录、输出全部随级别缩放;读取规则改为"按需 + 够用即停 + 会话缓存";README-代码系统性核对与 changes 压缩沉淀从日常任务剥离,交给新增的 maintainer 技能定期执行;builder 改为离线优先、P0 渐进覆盖。原因:v1 对所有任务执行同等重量的协议,低风险任务的流程成本远高于其不确定性收益。
- 已消除的不确定性:确认 v1 的主要负担来自每任务全量阅读、11 字段记录与每任务一致性核对;确认 `.ai/` 记录结构与不确定性协议在实际使用中有效,应保留。
- 关键假设:分级误判在元规则(拿不准即升级)下只会向更重方向发生;维护周期 2–4 周 / 15 条记录的默认阈值适合当前项目节奏,可由 maintainer 校准。
- 剩余不确定性:L0/L1 边界在具体项目中的松紧,需 1–2 个维护周期的实际记录来校准。
- 影响范围 / 风险控制:仅修改文档与技能,不改代码行为;v1 全部禁止行为与风险分级表保留。
- 验证方式:全文路径引用检查;分级表与模板在 README、AGENTS、两个技能间交叉一致。
- README 更新:是,根 README 重写为 v2。
- 是否新增/更新 .ai/decisions/:新增 0002-tiered-protocol.md,记录"按风险分级执行"这一长期决策。

## 10:02 - 升级 README First 至 v2.1 [L2]
- 用户目标:在不增加日常任务负担的前提下,补齐 v2 在验证可信度、框架可升级性、本质不确定性压缩和跨会话连续性上的四块短板。
- 涉及目录:根 `AGENTS.md`、根 `README.md`、`skills/readme-first-builder/`、`skills/readme-first-maintainer/`、`skills/readme-first-maintainer/scripts/`、`migrations/`、`.ai/architecture/`、`.ai/decisions/`、`.ai/glossary.md`、`.ai/handoff.md`、`.ai/plans/`。
- 修改内容与原因:引入语义版本(`VERSION` + 印记 + `migrations/`);新增验证证据门、访谈模式、术语表、计划协议、会话交接五项协议机制;builder 增加升级模式与热度×复杂度双因子选点;maintainer 增加三个只读巡检脚本并扩展巡检项;标准扩展(`.ai/plans/`、`.ai/glossary.md`、`.ai/handoff.md`)按需启用,不进入最小系统。原因:v2 已解决协议重量问题,但缺乏验证可审计性、升级通道、用户意图压缩手段和跨会话连续性。
- 已消除的不确定性:确认 v2 的最小系统(AGENTS.md、根 README、architecture、changes、decisions)应保持不变;确认 plans/glossary/handoff 有明确触发条件,不命中时不增加 L0/L1 负担;确认脚本只读、输出数据、Agent 判断的模式符合"不做修改"的安全边界。
- 关键假设:语义版本方案(MAJOR/MINOR/PATCH)能被下游项目正确印记;热度×复杂度双因子的默认阈值(heat≥5 & complexity≥10)在真实项目中可用;验证证据格式(命令+输出+结论)不会让 L1 小任务过度负担。
- 剩余不确定性:双因子阈值和访谈模式的触发边界需 1–2 个真实任务周期校准;脚本在 Windows Git Bash 外的环境中的稳定性待验证。
- 影响范围 / 风险控制:仅修改文档、技能、脚本和迁移说明,不改代码行为;所有新增组件均为触发式,不影响 L0/L1 必经路径;保留 v2 全部禁止行为与风险分级表。
- 验证证据:
  - `./skills/readme-first-maintainer/scripts/check-paths.sh .` → 输出 `check-paths: clean`
  - `./skills/readme-first-maintainer/scripts/check-freshness.sh .` → 输出 `check-freshness: clean`
  - `wc -l AGENTS.md` → 276 行(≤300);`sed -n '17,61p' AGENTS.md | wc -l` → 45 行(≤45);`wc -l README.md` → 145 行(≤145)
  - `cat VERSION` → 2.1.0;`head -1 AGENTS.md` → `<!-- README First protocol v2.1.0 -->
- README 更新:是,根 README 重写为 v2.1。
- 是否新增/更新 .ai/decisions/:新增 0003-versioning-and-extensions.md 与 0004-v2.1-mechanisms.md。
