## 15:37 - 升级 README First 至 v2.2 风险 Profile 与能力包架构 [L2]
- 用户目标:将数据库、事务、并发、网络和安全等全栈工程能力纳入 README First，使 Agent 不仅知道“读什么、怎么记录”，还知道高风险任务应加载什么专业能力、留下什么证据，并完成可审查、可验证的 GitHub PR。
- 涉及目录:根 `AGENTS.md`、根 `README.md`、`VERSION`、`migrations/`、`extensions/`、`skills/readme-first-builder/`、`skills/readme-first-maintainer/`、`.ai/architecture/`、`.ai/decisions/`、`.ai/glossary.md`、`tests/`、`.github/workflows/`。
- 风险领域:协议兼容性、Profile/Skill 路由、验证可信度、脚本可移植性、发布流程。
- 使用的 Profile / Skill:ReadmeFirst 仓库自身 L2 协议；GitHub 发布流程；新增 `fullstack-foundations` 作为 v2.2 参考能力包。
- 业务不变量与信任边界:
  - L0/L1/L2 仍只决定流程重量，不能被风险领域替代；
  - Profile 和能力包必须可选、渐进披露且不覆盖项目本地更严格规则；
  - Scanner 命中只是候选，不能作为已确认问题；
  - v2.1 下游项目在不安装 Profile 时仍可继续使用；
  - 没有新鲜验证证据不得声称协议升级完成。
- 修改内容与原因:
  - 协议升级到 2.2.0，引入 `risk_domains`、`.ai/profiles/`、`extensions/`、Profile/Skill 优先级和风险对应证据；
  - Builder 增加基于仓库事实的能力包候选发现、显式安装和自包含热点脚本；
  - Maintainer 增加 Profile/Skill/证据治理和 `check-profiles.py`；
  - 发布 `fullstack-foundations` Profile、CodeAgent Skill、审查清单、正反模式、输出契约、候选扫描器和渐进培训手册；
  - 修复 `check-freshness.sh` 管道子 shell 导致错误退出码的问题；修正 `dir-hotspots.sh` 的唯一提交热度与内部深度定义；
  - 将时效戳规范改为可在提交前准确填写的核验日期，并兼容旧格式；
  - 新增 fixture 回归测试、仓库不变量验证和 GitHub Actions。
- 已消除的不确定性:
  - 通过当前代码确认 `check-freshness.sh` 的状态在管道子 shell 中不能回传；
  - 通过 fixture 证明旧热点算法会把同一提交中的多个文件重复计数；
  - 通过同步校验确认根 `AGENTS.md` 与 Builder 离线模板一致；
  - 通过 Profile 校验确认 canonical Profile 与 Skill 绑定有效；
  - 通过迁移指南确认 v2.1 可按增量方式升级且不覆盖本地规则；
  - 首次远端 CI 发现 `.ai/glossary.md` 存在无效 UTF-8，修复后完整工作流通过。
- 关键假设:Markdown Profile 足以支撑 v2.2 的首轮真实使用；标准风险领域枚举需要在 1–2 个真实项目周期后校准；完整全栈能力包适合作为参考实现而非默认安装项。
- 剩余不确定性:能力包触发精度、不同 Agent 对 Profile 的加载一致性、Windows/Git Bash 兼容性和真实大型项目中的热点阈值仍需下游实测。
- 影响范围 / 风险控制:所有协议变化保持向后兼容；新目录为可选扩展；通过独立分支和 Draft PR 发布，不直接覆盖 `main`；不删除既有业务内容；脚本只读，测试使用临时仓库；不启用自动合并。
- 风险对应证据:
  - 脚本正确性:`bash tests/test-maintainer-scripts.sh` → `test-maintainer-scripts: all tests passed`；覆盖相对链接、代码块过滤、fresh/stale/no-stamp、退出码、唯一提交热度、Profile/Skill 正反例、`/tmp` 路径扫描和候选级别退出策略。
  - Python 语法:`bash tests/check-python-syntax.sh` → `check-python-syntax: clean (3 file(s))`。
  - 仓库契约:`python3 tests/validate_repository.py` → `validate-repository: clean (version 2.2.0, 3 skills, 10 training chapters, profile bindings valid)`。
  - 路径与时效:`check-paths.sh .`、`check-freshness.sh .` → 均输出 `clean`。
  - 远端 CI:PR #1 的 `validate` 工作流在修复 UTF-8 问题后全部步骤成功，包括 whitespace、shell/Python 语法、fixture、仓库契约、路径和文档时效检查。
  - 能力包完整性:1 个培训索引、10 个按需专题、Profile、Skill、检查表、模式库、输出契约、来源映射、示例和风险候选扫描器均纳入仓库验证。
- README / Profile 更新:是；根 README、architecture、Builder/Maintainer、Profile、能力包 README 和迁移指南均同步。
- 是否新增/更新 `.ai/decisions/`:新增 `0005-risk-domain-profiles-and-capability-packs.md`。
