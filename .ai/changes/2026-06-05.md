## 11:08 - 新增 README First 架构知识层

- 用户目标：参考 AutoWaterSimu `docs/architecture` 的组织方式，为 ReadmeFirst 新增通用 `.ai/architecture` 架构知识层并推送到 `main`。
- 涉及目录：`.ai/architecture/`、`.ai/changes/`、`.ai/decisions/`、根 `AGENTS.md`、根 `README.md`、`skills/readme-first-builder/`。
- 修改内容：新增架构入口、上下文地图、依赖边界、文档契约、builder skill 边界、本地维护流程、当前状态、变更记录和长期决策记录；同步更新根协议、README 和 skill canonical source。
- 修改原因：让 README First 不只定义 changes/decisions 记录机制，也有一个稳定的当前架构入口，方便后续 Agent 理解文档系统本身。
- 本次已消除的不确定性：确认 ReadmeFirst 当前根目录只有 `AGENTS.md`、`README.md`、`skills/`；确认 AutoWaterSimu 的可复用部分是架构目录组织方式而非业务内容；确认本次直接推送 `main`。
- 本次采用的关键假设：`.ai/architecture` 应纳入 README First 推荐最小系统；`.ai/changes` 和 `.ai/decisions` 需要用本次记录文件进入 git。
- 剩余不确定性：未来是否需要自动化链接检查或机器可解析 schema，暂不在本次实现。
- 影响范围 / 风险控制方式：仅修改文档和 skill 元数据，不引入依赖、不改代码运行行为。
- 验证方式：运行 `git diff --check`、路径引用 `rg` 检查和 `git status --short`。
- 是否更新 README：是，更新根 `README.md` 和 `.ai/architecture/README.md`。
- 是否需要新增或更新架构决策（.ai/decisions/）：是，新增 `.ai/decisions/0001-architecture-context-layer.md`。
